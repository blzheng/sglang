--- /tmp/pr_files/base/python/sglang/srt/layers/linear.py	2026-04-27 03:13:15.261180511 +0000
+++ /tmp/pr_files/head/python/sglang/srt/layers/linear.py	2026-04-27 03:13:15.257180513 +0000
@@ -371,9 +376,14 @@
             ),
         )
         if bias:
-            self.bias = Parameter(
-                torch.zeros(self.output_size_per_partition, dtype=params_dtype)
-            )
+            if _is_cpu:
+                self.bias = Parameter(
+                    torch.zeros(self.output_size_per_partition, dtype=params_dtype)
+                )
+            else:
+                self.bias = Parameter(
+                    torch.empty(self.output_size_per_partition, dtype=params_dtype)
+                )
             set_weight_attrs(
                 self.bias,
                 {
@@ -1413,7 +1423,10 @@
         )
 
         if bias:
-            self.bias = Parameter(torch.zeros(self.output_size, dtype=params_dtype))
+            if _is_cpu:
+                self.bias = Parameter(torch.zeros(self.output_size, dtype=params_dtype))
+            else:
+                self.bias = Parameter(torch.empty(self.output_size, dtype=params_dtype))
             set_weight_attrs(
                 self.bias,
                 {
